# Claude Run Environment Configuration
# Copy this file to .env.development or .env.production and fill in your values

# Server port
PORT=12001

# Session timeout in milliseconds (0 = disabled)
# Recommended: 1800000 (30 minutes) for production
CLAUDE_RUN_SESSION_TIMEOUT_MS=0

# Docker mode - set to 'true' when running in Docker container
CLAUDE_RUN_DOCKER=false

# SSH Configuration (required for Docker, optional otherwise)
# Path to SSH private key
# CLAUDE_RUN_SSH_KEY=/path/to/.ssh/id_rsa
# Path to known_hosts file
# CLAUDE_RUN_SSH_KNOWN_HOSTS=/path/to/.ssh/known_hosts
# Home directory for SSH (Docker only)
# CLAUDE_RUN_SSH_HOME=/home/claude-run

# Host Configuration
# Configure up to 2 hosts via environment variables
# Use ~/.claude-run/config.json for more complex setups

# Primary host (e.g., your main development machine)
# CLAUDE_RUN_HOST_PRIMARY_LABEL=MyServer
# CLAUDE_RUN_HOST_PRIMARY_TYPE=ssh
# CLAUDE_RUN_HOST_PRIMARY_HOST=192.168.1.100
# CLAUDE_RUN_HOST_PRIMARY_USER=myuser

# Secondary host (e.g., backup or secondary machine)
# CLAUDE_RUN_HOST_SECONDARY_LABEL=BackupServer
# CLAUDE_RUN_HOST_SECONDARY_TYPE=ssh
# CLAUDE_RUN_HOST_SECONDARY_HOST=192.168.1.101
# CLAUDE_RUN_HOST_SECONDARY_USER=myuser

# Default host ID: 'local', 'primary', or 'secondary'
# CLAUDE_RUN_DEFAULT_HOST=local
